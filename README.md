
# Discord YouTube Music Bot

## Setup
1. Install Node.js 18+
2. Copy `.env.example` to `.env`
3. Put your NEW bot token in `.env`
4. Run:
```
npm install
npm start
```

Command:
- `!play <YouTube URL>`
